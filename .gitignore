# env
.env*

# Next.js
.next/
out/

# Node
node_modules/
npm-debug.log*

# TypeScript artıklı derleme önbelleği — her build'de değişir, repoda işi yok
tsconfig.tsbuildinfo

# Claude Code yerel ayarları.
# Bu dosya makineye özeldir ve verilen izinlerin TAM KOMUT metnini saklar;
# içinde bağlantı dizesi/parola geçen komutlar birikebiliyor (bir kez
# DATABASE_URL parolasıyla dolu hâlde commit edilmeye çok yaklaştı).
# Paylaşılan ayarlar .claude/settings.json dosyasına yazılmalı.
.claude/settings.local.json

# Prisma
prisma/generated/

# Uploaded files (local dev only — use Vercel Blob in production)
/public/uploads/**
!/public/uploads/.gitkeep
!/public/uploads/hero/
!/public/uploads/hero/.gitkeep

# OS
.DS_Store
Thumbs.db

# PayTR kaynak paketleri (referans; repoya dahil edilmez — logolar public/payment'a alındı)
/PayTR_Gorselleri.zip
/PayTR_IFrame_API.zip
